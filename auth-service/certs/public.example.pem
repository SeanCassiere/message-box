-----BEGIN PUBLIC KEY-----
First you need to generate the private key. (Check the private.example.pem file)

Then generate the public key using the following command:

  openssl rsa -in private.pem -pubout -out public.pem 

  
-----END PUBLIC KEY-----
